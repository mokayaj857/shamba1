import React from 'react';

// App component is unused; routing is defined in src/main.tsx with RouterProvider.
// Keep this minimal to avoid import and routing duplication errors.
export default function App() {
  return <div />;
}